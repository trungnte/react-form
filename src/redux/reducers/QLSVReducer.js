const initialState = {}

export const QLSVReducer =  (state = initialState, action) => {
  switch (action.type) {

  // case first:
  //   return { ...state, ...payload }

  default:
    return state
  }
}
